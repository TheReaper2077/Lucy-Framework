#pragma once

#include "../OpenGL/OpenGL.h"
#include "../define.h"
// #include "Lucy.h"

// bool ImGuiTile(lf::Tile *tile) {
// 	return ImGui::ImageButton((void*)tile->tex->id, ImVec2(256, 256), ImVec2(tile->uv0.x, tile->uv0.y), ImVec2(tile->uv1.x, tile->uv1.y));
// }

// bool ImGuiTile(const char* name) {
// 	auto* tile = lf::GetTile(name);
// 	return ImGui::ImageButton((void*)tile->tex->id, ImVec2(256, 256), ImVec2(tile->uv0.x, tile->uv0.y), ImVec2(tile->uv1.x, tile->uv1.y));
// }