#pragma once

#include "../define.h"

typedef uint8_t BlockId;

enum BlockTypes {
	AIR,
	TRANSPARENT,
	SOLID,
};

struct Block {
	// BlockId;
};

class BlockRegistry {


public:
	BlockId AddBlock() {

	}
};